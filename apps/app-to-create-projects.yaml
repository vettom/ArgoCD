project: default
source:
  repoURL: 'https://github.com/vettom/ArgoCD.git'
  path: argo-projects
  targetRevision: HEAD
destination:
  server: 'https://kubernetes.default.svc'
  namespace: argocd
syncPolicy:
  automated:
    prune: true
    selfHeal: true
  syncOptions:
    - CreateNamespace=true
